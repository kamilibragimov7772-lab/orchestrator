# Раунд 2: независимые находки и исправления

Дата: 2026-09-06. Этот файл обновляет состояние отчётов 00–11, которые сохраняются как исторический срез `8c7cbd8`. Точный итоговый commit и результаты последующего CI — в `DELIVERY.json` внешнего локального пакета и GitHub Actions ветки `audit/hardening-9-5`.

## Что изменилось в доказательствах

Пользователь разрешил `/orchestr`; два независимых reviewer успели передать конкретные воспроизведения и противоречия, после чего их сессии завершились ошибкой лимита. Формальная независимая приёмка итогового кандидата не получена. По просьбе пользователя дальнейшие исправления и тестирование переданы `gpt-5.6-luna`, low; дорогие reviewer не перезапускались. Полный построчный семантический аудит всех 41 карточек не выполнен.

Ветка `audit/hardening-9-5` опубликована. Первый remote CI на `8c7cbd8810282f6e843c30ccf3fab26f67306466`: Linux/macOS на Python 3.10/3.12 и security прошли; оба Windows job упали на сравнении короткого и разрешённого пути temp-каталога в тесте установки. [Первый CI run](https://github.com/kamilibragimov7772-lab/orchestrator/actions/runs/34021076141). Результаты этого запуска не переносить автоматически на следующий SHA. Защита main не настроена.

## Находки раунда

| Находка | Исправление / проверяемое условие |
|---|---|
| Secret scan пропускал staged rename | Включены rename/type change и `--no-renames`; регрессия реального переименования в private settings |
| Sync мог заменить чужой staged blob после предварительной проверки | Реальный `index.lock` до проверки, отдельный Git index, публикация под своим lock; конкурентный Git client должен получить отказ |
| Doctor принимал неработающий guard/matcher | Проверка файла, interpreter, matcher; benign/secret/risk smoke, отрицательные сценарии wiring |
| Install оставлял часть файлов при file-parent collision | Полная проверка цепочек родителей stack/vault перед записью; отрицательный тест |
| Пустой transcript затирал существующий export | Отказ для пустого, malformed и non-record ввода до замены файла |
| Архивирование wildcard затрагивало все активные runs | Только текущий run_id и точные пути, проверка статуса и коллизии |
| Протокол предлагал обход безопасного sync | Убраны автоматические обещания и прямой `git add -A`; явный manifest, настроенный remote, проверка принимающего SHA |
| Minimal создавал впечатление полного pipeline | Указан поддерживаемый класс задач и preflight нужных ролей; расширенные роли требуют full |
| Приёмка противоречила сама себе о правах Edit и словаре | Уточнены два разрешённых изменения, отображение field/body verdict и status != done |

Regression tests и итоговый verify записываются в локальном пакете `review_round2/verification_final.txt`; CI проверяет опубликованный SHA отдельно. Исправления, найденные при root review младшего исполнителя, включают matcher doctor, scan имён private index и регрессию именно окна гонки, а не только существующего lock.

## Что остаётся открыто

1. `launch.py` всё ещё выдаёт модели Edit; граница prompt-only не обеспечивает изоляцию. Черновик writer вынесен из production checkout в локальный пакет и не подключён. Нужны tool-free structured reviewer, узкий writer и негативные проверки.
2. Нет live Claude E2E приёмки good/broken/failure, независимого итогового PASS и полной семантической приёмки карточек. Скриптовый PASS не заменяет их.
3. Branch protection main и контроль полного release scope остаются открыты. Optional media/site сценарии не аттестованы.
4. Живой private стек ноутбука/сервера не обновлялся. Read-only подтверждены actual `.claude` roots, отдельный private bare remote и Claude Code 2.1.261 на обеих машинах. Миграция описана в отдельном handoff MD.

Предварительные 8,8/10 из прошлого scorecard не пересчитываются вверх без новой приёмки. Доказанные 9,5 не достигнуты. Текущий результат — исправленный проверяемый кандидат и конкретный план завершения в Claude Code с ограничением расхода модели.

## Методологическая опора

NIST SSDF 1.1 (NIST, 2022): воспроизведение, исправление и проверка дефектов; SLSA 1.2 (2025): проверяемое происхождение поставки, без заявления уровня. [NIST](https://csrc.nist.gov/projects/ssdf), [SLSA](https://slsa.dev/spec/v1.2/), [Claude CLI](https://code.claude.com/docs/en/cli-reference). Проверено 2026-09-06. Балльная шкала — из исходного брифа, не стандарт NIST.
